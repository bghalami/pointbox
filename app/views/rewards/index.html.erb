<h1>Reward Shop</h1>

<% @rewards.each do |reward| %>
  <% if reward.disabled == 0 %>
    <ul>
      <li><%= reward.title %></li>
      <li>Price: <%= reward.price %></li>
      <li><%= link_to "Buy", users_buy_reward_path(current_user, reward) %></li>
    </ul>
  <% end %>
<% end %>
